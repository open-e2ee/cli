# `create-oe`

Run `npm create oe@latest` to create `open-e2ee.jsonc`, write a local example,
and execute a real SDK encrypted round trip against in-memory development
adapters. It does not require a login, card, or backend.

Managed Relay is under development and is not publicly available.
